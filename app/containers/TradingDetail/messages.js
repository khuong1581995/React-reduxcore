/*
 * TradingDetail Messages
 *
 * This contains all the text for the TradingDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TradingDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TradingDetail container!',
  },
});
